name: Portfolio Automation
on: [push]

jobs:
  audit_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Important: Allows the bot to save the report
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install google-generativeai nbformat pandas seaborn matplotlib nbformat

      - name: Run Gemini Audit
        env:
          # FIX: Put this on one line and match the name in your Python script
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python auto_audit.py

      - name: Save Report
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add DATA_AUDIT_REPORT.md
          git commit -m "Auto-generated data audit" || echo "No changes to commit"
          git push
